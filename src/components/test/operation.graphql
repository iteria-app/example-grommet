query AllUtilization {
  utilization(order_by: { name: asc }) {
    id
    value
    usedValue
    used
    percent
    name
    availableValue
    available
  }
}

query AllCustomers ($limit: Int) {
  customers(limit: $limit) {
    updatedAt
    phone
    name
    id
    email
    createdAt
    avatarUrl
    address
  }
}
